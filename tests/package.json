{
	"name": "samisphere-rules-tests",
	"description": "Firestore security-rules tests (emulator) for SamiSphere",
	"private": true,
	"type": "module",
	"scripts": {
		"test": "firebase emulators:exec --only firestore --project demo-samisphere --config ../firebase.json \"node firestore.rules.test.mjs\""
	},
	"devDependencies": {
		"@firebase/rules-unit-testing": "^3.0.4",
		"firebase": "^10.13.2"
	}
}
